[project]
name = "llm-router"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse>=1.4.0",
    "datasets>=4.8.4",
    "pytest>=9.0.2",
    "tiktoken>=0.12.0",
]
